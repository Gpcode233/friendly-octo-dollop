{
  "name": "veet-classroom",
  "version": "1.0.0",
  "description": "Virtual Classroom Portal",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html",
    "start": "live-server --port=3000 --open=index.html"
  },
  "dependencies": {
    "firebase": "^9.23.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}